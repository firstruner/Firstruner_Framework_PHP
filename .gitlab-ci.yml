# This file is a template, and might need editing before it works on your project.
# This is a sample GitLab CI/CD configuration file that should run without any modifications.
# It demonstrates a basic 3 stage CI/CD pipeline. Instead of real tests or scripts,
# it uses echo commands to simulate the pipeline execution.
#
# A pipeline is composed of independent jobs that run scripts, grouped into stages.
# Stages run in sequential order, but jobs within stages run in parallel.
#
# For more information, see: https://docs.gitlab.com/ee/ci/yaml/#stages
#
# You can copy and paste this template into a new `.gitlab-ci.yml` file.
# You should not add this template to an existing `.gitlab-ci.yml` file by using the `include:` keyword.
#
# To contribute improvements to CI/CD templates, please follow the Development guide at:
# https://docs.gitlab.com/development/cicd/templates/
# This specific template is located at:
# https://gitlab.com/gitlab-org/gitlab/-/blob/master/lib/gitlab/ci/templates/Getting-Started.gitlab-ci.yml
default:
  image: php:8.2.12-cli

variables:
  GIT_TERMINAL_PROMPT: "0"

stages:
  - test
  - pushToGitHub
  - notify

loader-test:
  stage: test
  script:
    - php ./loader.php

push_to_github:
  stage: pushToGitHub
  image: alpine:3.20
  variables:
    GIT_TERMINAL_PROMPT: "0"
  before_script:
    - apk add --no-cache bash git python3 py3-pip
    # venv (contourne PEP 668)
    - python3 -m venv /tmp/venv
    - . /tmp/venv/bin/activate
    - pip install --no-cache-dir git-filter-repo
    # vérif
    - git filter-repo --version
    - test -n "$GITHUB_USER"
    - test -n "$GITHUB_TOKEN"
  script:
    - . /tmp/venv/bin/activate
    - find . -maxdepth 3 -type f -name "*push*github*.sh" -print
    - bash ./pushtogithub.sh
  only:
    - main

discord_stagiaires:
  stage: notify
  image: python:3.12-alpine
  only:
    - main
  before_script:
    - apk add --no-cache curl
  script:
    - |
      python3 - <<'PY'
      import json, os, subprocess

      content = (
        "✅ Firstruner Framework PHP : Nouvelle version !\n"
        f"Pipeline message : loader réussi\n"
        f"Description : {os.getenv('CI_COMMIT_MESSAGE','')}\n"
        f"Tags : {os.getenv('CI_COMMIT_TAG') or 'aucun'}\n\n"
        f"🌐 Private repository : https://gitlab.com/firstruner/Firstruner_Framework_PHP\n"
        f"🌐 Public repository : https://github.com/firstruner/Firstruner_Framework_PHP"
      )

      payload = json.dumps({
        "username": "Botty via GitLab CI/CD",
        "content": content
      })

      subprocess.run(
        ["curl", "-sS", "-X", "POST",
         "-H", "Content-Type: application/json",
         "-d", payload,
         os.environ["DISCORD_WEBHOOK_URL_STG"]],
        check=True
      )
      PY
      
discord_firstruner:
  stage: notify
  image: python:3.12-alpine
  only:
    - main
  before_script:
    - apk add --no-cache curl
  script:
    - |
      python3 - <<'PY'
      import json, os, subprocess

      content = (
        "✅ Firstruner Framework PHP : Nouvelle version !\n"
        f"Pipeline message : loader réussi\n"
        f"Description : {os.getenv('CI_COMMIT_MESSAGE','')}\n"
        f"Tags : {os.getenv('CI_COMMIT_TAG') or 'aucun'}\n\n"
        f"🌐 Private repository : https://gitlab.com/firstruner/Firstruner_Framework_PHP\n"
        f"🌐 Public repository : https://github.com/firstruner/Firstruner_Framework_PHP"
      )

      payload = json.dumps({
        "username": "Botty via GitLab CI/CD",
        "content": content
      })

      subprocess.run(
        ["curl", "-sS", "-X", "POST",
         "-H", "Content-Type: application/json",
         "-d", payload,
         os.environ["DISCORD_WEBHOOK_URL_1ST"]],
        check=True
      )
      PY